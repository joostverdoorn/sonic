describe "Tree", ->

  it "should work", ->

    # tree = new Sonic.Tree()

    # nodes = []
    # for i in [0...10]
    #   nodes[i] = new Sonic.TreeNode( i )
    #   tree.insert(nodes[i])

    #   expect(tree.findPosition(i)).toBe(nodes[i])

    # expect(nodes[9].parent()).toBe(nodes[8])

    # console.log JSON.stringify tree

